// import React, { useMemo } from "react";
import styles from "./EqualizerAnimation.module.scss";

export default function EqualizerAnimation() {
  // const bars = useMemo(() => Array.from({length: barsCount}, (x, i) => i), [
  //   barsCount,
  // ])

  return <div className={styles["equalizer"]}></div>;
}
